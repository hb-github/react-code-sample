import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { AppRouter } from './router';

//Rendering App main component in Dom 
ReactDOM.render(<AppRouter/>, document.getElementById('root'));
